# @andrewjypark/subatomic-design-tokens-chocolate

Chocolate theme CSS design tokens for the Subatomic design system.

## Install

```bash
npm install @andrewjypark/subatomic-design-tokens-chocolate
```

## Usage

Import the CSS custom properties into your stylesheet:

```css
@import "@andrewjypark/subatomic-design-tokens-chocolate/css/_variables.css";
@import "@andrewjypark/subatomic-design-tokens-chocolate/css/abstracts.css";
```

- `css/_variables.css` — generated design tokens (colors, spacing, typography, etc.)
- `css/abstracts.css` — base/breakpoint variables shared across all flavors

## License

MIT
